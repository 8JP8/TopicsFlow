# Example environment file for TopicsFlow frontend
# Copy to frontend/.env.local and fill in real values.

NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_APP_NAME=TopicsFlow

# Optional (only if your frontend uses it directly)
NEXT_PUBLIC_TENOR_API_KEY=


